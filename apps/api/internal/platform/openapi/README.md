# OpenAPI

The MVP starts OpenAPI-first because Go is not available locally for generated annotation workflows.

The canonical spec will live at:

```txt
apps/api/internal/platform/openapi/openapi.yaml
```

The generated TypeScript client will live in:

```txt
packages/api-client
```
